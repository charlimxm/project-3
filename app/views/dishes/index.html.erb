<div class="container">
  <h1>Dishes</h1>
<div class="row">
<% @dishesSorted.each do |dish, rating| %>

  <div class="col s12 m6 l3">
    <div class="card medium">
      <div class="card-image">
        <%= link_to image_tag(dish.photourl), dish_path(dish.id) %>
      </div>
      <div class="card-content">
        <span class="card-title"><%= dish.name %></span>
        <p>restaurant name </p>
        <p> at location</p>

        <div class="card-action">
          <%= form_tag ratings_path, method: :post, remote:true do %>
            <%= hidden_field_tag :dish_id, dish.id %>
            <%= hidden_field_tag :rating, rating %>
            <%= button_tag( class: "btn") do %>
            <i class="material-icons">thumb_up</i>
            <% end %>
            <label class="ratingPara" id="ratingPara<%= dish.id %>"> <%= rating %> vote(s)</label>
          <% end %>
        </div>
        </div>
    </div>
  </div>

<% end %>
</div>
</div>
