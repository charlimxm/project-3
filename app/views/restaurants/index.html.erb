<div class="container center">

<h1> All Restaurants</h1>
<div class="collection">
  <% @restos.each do |rest| %>
        <%= link_to "#{rest.name}", restaurant_path(rest.id), class: "collection-item" %>
<% end %>
  </div>
</div>
